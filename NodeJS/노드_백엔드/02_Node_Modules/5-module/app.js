import { increase, getCount } from './counter.js';
// import * as counter from './counter.js';  // 이렇게 쓰면 밑에 함수 쓸 때 counter.increase(); 이런 식으로

increase();
increase();
increase();
console.log(getCount());